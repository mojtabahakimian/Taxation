# راهنمای کار روی این مخزن

**اگر تازه به این پروژه رسیده‌ای، این فایل را کامل بخوان. طول می‌کشد، ولی
کوتاه‌تر از وقتی است که صرف کشف دوبارهٔ همین چیزها می‌شود.**

---

## ۰. این نرم‌افزار چه کار می‌کند

صورتحساب‌های فروش را از پایگاه دادهٔ یک نرم‌افزار حسابداری برمی‌دارد، به قالب
**سامانه مؤدیان** سازمان امور مالیاتی ایران تبدیل می‌کند، رمزنگاری می‌کند و به
API دولتی می‌فرستد، سپس وضعیتشان را پیگیری می‌کند.

**پول واقعی و تعهد مالیاتی واقعی درگیر است.** یک صورتحساب ردشده یعنی مؤدی
جریمه می‌شود. یک صورتحساب اشتباهاً دوبار ثبت‌شده یعنی باید دنبال ابطالش بدود.

پشتهٔ فنی: **C# / .NET 6 / WPF** روی **SQL Server**. رابط کاربری فارسی و
راست‌به‌چپ است.

### چهار پروژه

| پروژه | نقش |
|---|---|
| `Prg_Moadian` | **هستهٔ کتابخانه‌ای.** قواعد، مدل‌ها، رمزنگاری، مسیرهای ارسال. UI ندارد |
| `Prg_Grpsend` | برنامهٔ ارسال گروهی (لیست فاکتورها، انتخاب، ارسال دسته‌ای) |
| `Prg_TrackSentInvoice` | برنامهٔ پیگیری: وضعیت، استعلام، فرم اصلاحی، دکمهٔ ارسال مجدد |
| `Prg_Graphicy` | گزارش‌های گرافیکی — به مسیر مؤدیان کاری ندارد |

### واژه‌های دامنه که همه‌جا می‌بینی

| واژه | یعنی |
|---|---|
| **مؤدی** | مؤدی مالیاتی، همان فروشنده |
| **کارپوشه** | داشبورد آنلاین مؤدی در سایت سازمان — جایی که صورتحساب «می‌نشیند» |
| **حافظهٔ مالیاتی** | شناسهٔ ۶ کاراکتریِ صادرکننده که سازمان می‌دهد (`MEMORYID`) |
| **صورتحساب ارجاعی** | صورتحساب اصلاحی، برگشت از فروش، یا ابطالی — به یک مرجع اشاره می‌کند |

### فیلدهای کلیدی (نام‌های JSON روی سیم)

```
taxid    شماره منحصربه‌فرد مالیاتی، ۲۲ کاراکتر — کلید یکتایی کل سامانه
inno     سریال داخلی صورتحساب، ۱۰ کاراکتر
irtaxid  شماره مالیاتی صورتحساب مرجع (فقط در ارجاعی‌ها)
ins      موضوع:   ۱ اصلی   ۲ اصلاحی   ۳ ابطالی   ۴ برگشت از فروش
inty     نوع:     ۱ (خریدار مشخص، اعتبار مالیاتی دارد)   ۲ (مصرف‌کننده نهایی)
inp      الگو:    ۱ فروش عادی ... ۷ صادرات ... ۱۱ بورس
setm     تسویه:   ۱ نقدی   ۲ نسیه   ۳ نقدی/نسیه
tob      نوع شخص خریدار: ۱ حقیقی  ۲ حقوقی  ۳ مشارکت مدنی  ۴ اتباع غیرایرانی
tinb     شماره اقتصادی خریدار      bid  شناسه/شماره ملی خریدار
indatim  تاریخ و زمان صدور (میلادی، اپک میلی‌ثانیه)
indati2m تاریخ و زمان ثبت — فاصلهٔ این دو، «مهلت ارسال» را می‌سازد
insr     قاعدهٔ ارسال (ماده ۹) — برای ارسال خارج از مهلت
```

**ساختار پاسخ خطا:** رقم اول کد، ماهیتش را می‌گوید — `0` یعنی **خطا** (سند رد
می‌شود)، `1` یعنی **هشدار** (سند ثبت می‌شود). هشدارها را هرگز به بلاک تبدیل نکن.

---

## ۱. ⚠️ قانون اول: چیزی که کار می‌کرده را دست نزن

بخش زیادی از منطق این کد **به قیمت صورتحساب‌های ردشده** یاد گرفته شده. کدی که
اضافی، زشت یا اشتباه به نظر می‌رسد، اغلب عمدی است و توضیحش بالای خودش نوشته شده.

پیش از هر «تمیزکاری»، این‌ها را بخوان:

- `Prg_Moadian/FUNCTIONS/MoadianRules.cs` — قواعد مشترک، همه با ارجاع به صفحهٔ دستورالعمل
- کامنت‌های فارسیِ بالای هر محاسبهٔ مبلغ در `SendInvoiceBulk.cs` و `CL_MOADIAN.cs`

نمونه‌هایی که **حتماً** باید دست‌نخورده بمانند:

| چیز | چرا |
|---|---|
| `Random.Shared` در `TaxService.RequestTaxId` | عمدی است؛ رفع خطای `0300101` بعد از ابطالی که ناشی از `inno` تکراری است |
| `Math.Truncate` در هر محاسبهٔ مبلغ | تبدیلش به `Round` مبالغ را جابه‌جا می‌کند و سامانه رد می‌کند |
| باند هگز سریال از `0xA00000` | جلوی تداخل سریال فاکتورهای بالای یک میلیون را می‌گیرد |
| `AutoArticle9 = false` | روشن‌شدنش سند را «موضوع ماده ۹» اعلام می‌کند — تبعات جریمه‌ای دارد و باید با تأیید کاربر باشد |
| هشدارهایی که «ادامه می‌دهید؟» می‌پرسند | سامانه ثبات ندارد؛ نباید هیچ‌وقت تبدیل به بلاک شوند |
| تگ‌های `Prg_Grpsend` | گرید با تگ سرگروه کار می‌کند، ارسال با تگ حواله — عمدی است، بخش ۴ را بخوان |
| `Ins = 3` بدون بدنه | سند ص۱۷ می‌گوید اقلام از مرجع واکشی می‌شوند |

---

## ۲. اجرای تست — قبل و بعد از هر تغییر

```powershell
.\TestTools\LocalMoadian\run-tests.ps1          # سریع، بدون دیتابیس
.\TestTools\LocalMoadian\run-tests.ps1 -Full    # کامل، با دیتابیس
```

کد خروجی صفر یعنی سالم. اسکریپت خودش یک **سرور مؤدیانِ ساختگی محلی** (پایتون،
با رمزنگاری واقعی) بالا می‌آورد، داده تست می‌سازد و در پایان پاک می‌کند.

`-Full` پیش و پس از اجرا یک **اثر انگشت SHA-256 از کل محتوای `TAXDTL`** می‌گیرد
و اگر یک بایت از دادهٔ واقعی عوض شده باشد، شکست می‌دهد.

**روال درست هنگام تغییر کد:**

1. اول `-Full` را اجرا کن و مطمئن شو همه سبز است
2. تغییرت را بده
3. دوباره `-Full` را اجرا کن
4. اگر چیزی قرمز شد، **پیام تست را بخوان** — می‌گوید چه چیزی و چرا نباید عوض می‌شد
5. فقط وقتی مرجع را دوباره ثبت کن (`-Record`) که **مطمئنی** رفتار جدید درست‌تر است،
   و دلیلش را در پیام commit بنویس

پارامترها: `-Port` `-Server` `-Database` هم دارد. پیش‌فرض دیتابیس
`YAZDSEPAR1405_06_25` روی `MERCEDES\SQL2022` است.

---

## ۳. تست‌ها چه چیزی را پوشش می‌دهند

۳۷۲ تست در `TestTools/LocalMoadian/`.

| گروه | موضوع | نیاز |
|---|---|---|
| ۱ | سریال صورتحساب (Inno) | — |
| ۲ | قواعد مشترک MoadianRules | — |
| ۲۰ | **قفل رفتار** — تصمیم‌های عمدی که نباید عوض شوند | — |
| ۲۳ | ریاضیات فرم صورتحساب اصلاحی | — |
| ۲۷ | **احکام واقعی سامانه** از نصب «مروارید» | — |
| ۳ تا ۱۲ | ارسال، هویت خریدار، تاریخ، مبالغ، اصلاحی، ابطالی، استعلام | سرور ساختگی |
| ۱۳ تا ۱۵ | نوع صورتحساب، روش تسویه، ماتریس کامل | سرور ساختگی |
| ۱۷ | سازگاری با SDK و قالب JSON روی سیم | سرور ساختگی |
| ۲۶ | شبیه‌سازی زنده: عوارض، نقدی/نسیه، ارسال مجدد | سرور ساختگی |
| ۱۶ | ارسال گروهی سنگین، فاکتور بالای یک میلیون | دیتابیس |
| ۱۸ | دو محصول: MrCorrect و DenaFaraz | دیتابیس |
| ۱۹ | **تثبیت رفتار (Golden)** — payload دقیق نباید عوض شود | دیتابیس |
| ۲۱ / ۲۲ | مسیر تکی از سر تا ته / مقایسه‌اش با گروهی | دیتابیس |
| ۲۴ | دکمهٔ ارسال مجدد فاکتور | جزئی |
| ۲۵ | دیده‌بان‌های یافته‌های سند | دیتابیس |

گروه ۱۹ payload واقعیِ چند فاکتور را با `golden/*.json` مقایسه می‌کند؛ اگر یک
مبلغ یا یک گِردکردن عوض شود، دقیقاً می‌گوید کدام.

**قید مهم دربارهٔ گروه ۲۶:** سرور ساختگی را ما از روی متن سند نوشته‌ایم و کد
تولید را با آن می‌سنجیم. پس این گروه **هم‌خوانی دو پیاده‌سازی از یک متن** را
نشان می‌دهد، نه رفتار سامانهٔ واقعی را. نام تست‌هایش عمداً با «سرور ساختگی:»
شروع می‌شود. گروه ۲۷ برعکس است: آنجا حکم، حکمِ سامانهٔ واقعی است.

توضیح کامل در `TestTools/LocalMoadian/README.md`.

---

## ۴. نکات ساختاری که باید بدانی

### دو محصول، یک کد

```
CL_Generaly.MrCorrect = "m"   →  MrCorrect   (پیش‌فرض)
CL_Generaly.MrCorrect = "d"   →  DenaFaraz   (کلید لایسنس تشخیص می‌دهد، CL_LOCKWATCH.cs)
```

شرط `CALLER_NAME == CL_Generaly.MrCorrect` در عمل یعنی «آیا دناافراز نیستیم».

### تگ‌ها

```
TAG 2   = حواله انبار فروش   → ردیف‌های کالا اینجاست، همین به مؤدیان می‌رود
TAG 13  = سرگروه فاکتور فروش  → فقط سرصفحه
HEAD_BACK_ANBAR  = ویو روی HEAD_LST با «TAG - 11»
MrCorrect هر دو را دارد، DenaFaraz فقط ۲ را.
```

**`TAXDTL.TAG` همیشه ۲ نیست — می‌تواند `NULL` باشد.** (۱۷۶ ردیف در مروارید، ۱
ردیف در یزدسپار.) هر شرط `TAG = 2` یا `TAG NOT IN (...)` این‌ها را بی‌صدا
می‌اندازد، چون در SQL هر مقایسه‌ای با `NULL` نتیجه‌اش `NULL` است. اگر لازم شد،
`ISNULL(TAG, -1)` بنویس.

**نکته‌ای که ممکن است باگ به نظر برسد ولی نیست:** در `Prg_Grpsend` گرید و
فیلترها با تگ سرگروه کار می‌کنند (`IsDenafaraz ? "2" : "13"`) در حالی که
`TAXDTL.TAG` دو است. یعنی در MrCorrect فیلترِ «قبلاً ارسال شده» عملاً چیزی پیدا
نمی‌کند و همهٔ فاکتورها در لیست می‌مانند.

این آگاهانه نگه داشته شده — روند کاری سال‌هاست روی همین شکل بنا شده و عوض
کردنش ۳٬۴۱۱ فاکتور را از لیست پنهان می‌کند. **دست نزن.** تست ۱۸-۱۰ و ۲۰-۴۳
همین وضعیت را قفل کرده‌اند.

### چهار مسیر ارسال مستقل

```
گروهی       Prg_Moadian/Bulk/SendInvoiceBulk.cs          ← بهترین پوشش تست
تکی         Prg_Moadian/FUNCTIONS/CL_MOADIAN.cs
اصلاحی      Prg_TrackSentInvoice/WIN_MODIFYINVOICE.xaml.cs
ارسال مجدد  Prg_TrackSentInvoice/MainWindow.xaml.cs — RESEND_BTN
```

فرمول‌های مبلغ در سه مسیر اول **تکرار شده‌اند**. اگر یکی را عوض کردی، آن دوتای
دیگر را هم چک کن. گروه ۲۲ همین را خودکار می‌کند.

ریاضیات فرم اصلاحی به `Prg_Moadian/FUNCTIONS/CorrectionMath.cs` منتقل شده تا
تست‌پذیر باشد؛ گروه ۲۳ قفلش می‌کند.

**یک واگرایی شناخته‌شده و ثبت‌شده:** مسیر تکی و گروهی مالیات را فقط وقتی مقدار
ذخیره‌شده بزرگتر از صفر باشد بازمحاسبه می‌کنند؛ فرم اصلاحی بی‌قید و شرط. تست
۲۳-۲۰ این رفتار فعلی را ثبت کرده.

### ارسال مجدد (`RESEND_BTN`)

صورتحساب تازه نمی‌سازد — ردیف‌های `TAXDTL` نسخهٔ پایه را **عیناً** دوباره
می‌فرستد: همان `taxid`، همان `inno`، همان تاریخ. چون سامانه گاهی روزها روی
`PENDING` می‌ماند، یا رد می‌شود چون تکراری است یا پذیرفته می‌شود چون بار اول
نرسیده بود.

**شماره مالیاتیِ یکسان همان چیزی است که این دکمه را بی‌خطر می‌کند.** با شماره
مالیاتی تازه، همان فاکتور **دو بار** در کارپوشه می‌نشیند و باید یکی ابطال شود.
تست ۲۴-۱۳ همین را قفل کرده.

دام دیگر: `InsertNewTaxDtlRecord` باید **همهٔ** ستون‌ها را ببرد، از جمله
`NUMBER` و `TAG`. هر دو nullable‌اند، پس جا افتادنشان خطا نمی‌دهد — ولی گرید
پیگیری با `NUMBER` به `HEAD_LST` وصل می‌شود و ردیف بدون شمارهٔ فاکتور و بدون
نام مشتری ظاهر می‌شود. گروه ۲۴ قفلش کرده.

---

## ۵. قواعد سامانه که راستی‌آزمایی شده‌اند

منابع: دستورالعمل `RC_IITP.IS_V7.9`، سورس SDK رسمی، و دادهٔ عملیاتی سه نصب.
اسناد در `Prg_Grpsend/InfoDocuments/` و روی دسکتاپ کاربر.

### تأیید شده — دست نزن

| موضوع | شاهد |
|---|---|
| برش (`Math.Truncate`) نه گردکردن | «با روش قطع کردن محاسبه» + ۹۰۳ قلمِ تمایزدهنده در یک نصب مستقل، صفر نقض‌مثال |
| شرط وضعیت مرجع **فقط** وقتی مرجع خودش اصلاحی/برگشتی باشد | عین متن ص۱۶ بند ۳ |
| ابطالی نمی‌تواند مرجع باشد | ص۱۷ بند ۴-۲ |
| بدنهٔ ابطالی لازم نیست | ص۱۷: «لزومی به درج آن‌ها در فایل جیسون نمی‌باشد» |
| `cap + insp = tbill − tvam − todam` **فقط** برای `setm=3` | ص۴۶ جدول ۲۵، ص۴۷ جدول ۲۷؛ ص۴۵ این دو فیلد را فقط برای نقدی/نسیه اجباری می‌کند |
| سریال هگز در `inno` | نمونهٔ رسمی JSON: `"inno":"0002F2B4E7"` |
| بخش تصادفی در `taxid` مجاز است | SDK رسمی هم همین کار را می‌کند |
| بسته‌های حداکثر ۱۰۰تایی (بدون امضا) | ما ۹۹ می‌فرستیم |
| طول `tinb` وابسته به `tob`: حقیقی ۱۴، حقوقی ۱۱ | سامانه **عین الگو** را در متن خطا نوشت |
| ارسال مجدد با `taxid` تکراری رد می‌شود و بازنویسی نمی‌کند | سند + تجربهٔ عملی |

### امضا

SDK رسمی ۰.۰.۳۰ با `SignData(bytes, SHA256, RSASignaturePadding.Pkcs1)` امضا
می‌کند، پس از نرمال‌سازی «فلت‌کردن JSON، مرتب‌سازی الفبایی کلیدها، چسباندن با
`#`». در هدرهای `signature` و `signatureKeyId` می‌رود — **JWS نیست.**

**ما اصلاً امضا نمی‌کنیم.** `CL_ORIGINAL_SDK.SignData` و نرمال‌سازش نوشته شده‌اند
ولی هرگز صدا زده نمی‌شوند. همین با سقف ۱۰۰تاییِ بسته‌های بدون امضا می‌خواند.

### مهلت ارسال

مهلت ۱۲ روزه (از ۱۵ آبان ۱۴۰۳، کاهش از ۲۱ روز). مرز واقعی در داده، برای **نوع
اول**: موفق تا **۱۳** روز، ردشده از **۱۴** روز — جدایی کامل روی ۵۳۴ فاکتور بدون
یک استثنا. آستانهٔ ما (`SendDeadlineDays = 12`) یک روز محافظه‌کارانه‌تر است و چون
فقط هشدار می‌دهد بی‌ضرر. برای **نوع دوم مرز روشن نیست** — قاعده‌اش را قفل نکن.

### دو واگرایی نهفته — امروز بی‌اثر، ولی ثبت‌شده

۱. **عوارض.** ص۷۳ جدول ۵۳ ردیف ۱: `Os = Ks + Is + Ks2 + Ks3`، یعنی
   `tsstam = adis + vam + odam + olam`. ما `odam`/`olam` را کنار می‌گذاریم و
   `tbill` را هم بدون `todam` می‌سازیم. **امروز فرقی نمی‌کند** چون در هر
   ۱۸٬۷۳۶ ردیفِ سه دیتابیس این مقادیر صفرند. تست‌های ۲۵-۱ تا ۲۵-۳ دیده‌بانند:
   اولین مقدار غیرصفر، قرمز می‌کند و آن‌وقت باید تصمیم گرفت.

۲. **`setm=3`** هرگز در هیچ نصبی استفاده نشده (صفر فاکتور). فرمولش با ص۴۶/۴۷
   می‌خواند ولی شاهد عملیاتی ندارد. تست ۲۵-۵ ثبتش کرده.

### آنچه نمی‌دانیم — حدس نزن

- رفتار واقعی سامانه با `odam`/`olam` غیرصفر
- شکست‌های مسیر **اصلاحی** (`Ins=2`) — در مروارید صفر نمونه دارد
- مرز مهلت برای نوع دوم
- `InquiryInvoiceStatus` — در SDK ۰.۰.۳۰ **وجود ندارد**، هرچند در انجمن‌ها نامش می‌آید

---

## ۶. سه نصب واقعی، و آنچه یاد دادند

| دیتابیس | محصول | وضعیت |
|---|---|---|
| `YAZDSEPAR1405_06_25` | MrCorrect | مرجع اصلی — ۱۵٬۵۷۰ ردیف، ۳٬۴۱۲ موفق |
| `Morvarid1404` | MrCorrect | ۱۲۶۸ فاکتور، ۱۴۸ ردشده — گنجینهٔ خطا |
| `DENAF1405` | (نامشخص) | **خالی.** صفر ردیف، بدون کلید، هشت ستون کم دارد — یک restore قدیمی است، نه نمونهٔ محصول دناافراز |

کل ۱۴۸ شکست مروارید فقط سه کد است:

```
00012    فیلد «شماره اقتصادی خریدار» خالی است            ۶۵ فقره
0101204  الگوی ^\d{14}$ یا ^\d{11}$ رعایت نشده           ۴۲ فقره
02002    تاریخ و زمان صدور معتبر نیست                    ۴۱ فقره
```

**`MoadianRules.ValidateBuyer` روی هر ۱۴۸ حکم با سامانه هم‌نظر است** — صفر مورد
از دست رفته، صفر مثبت کاذب. گروه ۲۷ این را دائمی کرده. نکتهٔ ظریف: ۹ فقره از
مقادیر «۱۲ کاراکتری» اصلاً رقم نبودند — کاربر **شمارهٔ تلفن با خط تیره** در
فیلد شناسهٔ اقتصادی زده بود.

**`Ins=3` تنها مسیری است که واقعاً به بن‌بست می‌رسد:** ۶ مورد از ۷ فاکتوری که
هرگز موفق نشدند ابطالیِ `02002`اند — در حالی که ابطالی‌های موفق *همان شکل تاریخ*
را دارند و یکی‌شان دو دقیقه بعد با payload یکسان پذیرفته شد. یعنی `02002` روی
ابطالی اغلب **گذراست**، نه ساختاری.

**`taxid` به ازای هر تلاش تولید می‌شود، نه هر فاکتور.** در مروارید ۱۲۶۸ `taxid`
در برابر ۱۰۸۴ `inno`؛ هیچ `taxid` ای دو وضعیت متفاوت ندارد. هر منطق «قبلاً
ارسال شده؟» که روی `taxid` کلید بزند، تلاش‌های مجدد را چندبار می‌شمارد.

---

## ۷. بیلد

```powershell
& "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\MSBuild\Current\Bin\MSBuild.exe" TaxationApp.sln /v:m /nologo
```

`dotnet build` روی پروژهٔ `Prg_Grpsend` شکست می‌خورد (ارجاع COM قدیمی). از MSBuild
ویژوال استودیو استفاده کن. هارنس تست خودش با `dotnet build` بیلد می‌شود.

---

## ۸. دام‌های محیط

- **PowerShell 5.1 و فارسی:** فایل `.ps1` باید **UTF-8 با BOM** باشد وگرنه متن
  فارسی خراب می‌شود.
- **`sqlcmd` و فارسی:** `-W` متن فارسی را خراب می‌کند. برای خواندن درست:
  `-u` با `-o file.txt` بنویس و فایل را با `utf-16-le` بخوان.
- **`sqlcmd` و کد خروجی:** بدون `-b` حتی با خطا صفر برمی‌گرداند.
- دیتابیس‌ها `Arabic_CI_AS` هستند.

---

## ۹. commit

بدون هماهنگی صریح کاربر **commit یا push نکن**. خواندن و تغییر کد آزاد است.

---

## ۱۰. روش کار روی این پروژه

- **ادعا را با شاهد جفت کن.** «کد درست است» بدون اجرا، استنتاج است نه تأیید.
  اگر اجرایش نکرده‌ای، بگو نکرده‌ای.
- **تست باید خرابکاری را بگیرد.** بعد از نوشتن هر تست مهم، عمداً کد را خراب کن
  و ببین قرمز می‌شود. تستی که همیشه سبز است، تست نیست.
- **سرور ساختگی سامانه نیست.** اگر قاعده‌ای را هم در سرور ساختگی و هم در کد
  تولید از یک متن نوشته‌ای، هم‌خوانی‌شان چیزی را اثبات نمی‌کند.
- **دادهٔ عملیاتی از تفسیر سند قوی‌تر است.** وقتی سه منبع سه حرف می‌زنند،
  فاکتورهایی که سامانه واقعاً پذیرفته یا رد کرده داور نهایی‌اند.
- **مقادیر صفر چیزی اثبات نمی‌کنند.** اگر ستونی همه‌جا صفر است، دربارهٔ رفتار
  غیرصفرش هیچ نمی‌دانی. دیده‌بان بگذار، فرمول را حدسی عوض نکن.
