# راهنمای کار روی این مخزن

این نرم‌افزار صورتحساب الکترونیکی به سامانه مؤدیان جمهوری اسلامی ایران می‌فرستد.
پول واقعی و تعهد مالیاتی واقعی درگیر است.

---

## ⚠️ قانون اول: چیزی که کار می‌کرده را دست نزن

بخش زیادی از منطق این کد **به قیمت صورتحساب‌های ردشده** یاد گرفته شده. کدی که
اضافی، زشت یا اشتباه به نظر می‌رسد، اغلب عمدی است و توضیحش بالای خودش نوشته شده.

پیش از هر «تمیزکاری»، این‌ها را بخوان:

- `Prg_Moadian/FUNCTIONS/MoadianRules.cs` — قواعد مشترک، همه با ارجاع به صفحه دستورالعمل
- کامنت‌های فارسیِ بالای هر محاسبه‌ی مبلغ در `SendInvoiceBulk.cs` و `CL_MOADIAN.cs`

نمونه‌هایی که **حتماً** باید دست‌نخورده بمانند:

| چیز | چرا |
|---|---|
| `Random.Shared` در `TaxService.RequestTaxId` | عمدی است؛ رفع خطای `0300101` بعد از ابطالی |
| `Math.Truncate` در هر محاسبه مبلغ | تبدیلش به `Round` مبالغ را جابه‌جا می‌کند و سامانه رد می‌کند |
| باند هگز سریال از `0xA00000` | جلوی تداخل سریال فاکتورهای بالای یک میلیون را می‌گیرد |
| `AutoArticle9 = false` | اگر روشن شود ماهیت حقوقی هر فاکتور قدیمی عوض می‌شود |
| هشدارهایی که «ادامه می‌دهید؟» می‌پرسند | سامانه ثبات ندارد؛ نباید هیچ‌وقت تبدیل به بلاک شوند |
| تگ‌های `Prg_Grpsend` | گرید با تگ سرگروه کار می‌کند، ارسال با تگ حواله — عمدی است، پایین را بخوان |

---

## اجرای تست — قبل و بعد از هر تغییر

```powershell
.\TestTools\LocalMoadian\run-tests.ps1          # سریع، بدون دیتابیس
.\TestTools\LocalMoadian\run-tests.ps1 -Full    # کامل، با دیتابیس
```

کد خروجی صفر یعنی سالم. اسکریپت خودش سرور ساختگی را بالا می‌آورد، داده تست
می‌سازد و در پایان پاک می‌کند.

**روال درست هنگام تغییر کد:**

1. اول `-Full` را اجرا کن و مطمئن شو همه سبز است
2. تغییرت را بده
3. دوباره `-Full` را اجرا کن
4. اگر چیزی قرمز شد، **پیام تست را بخوان** — می‌گوید چه چیزی و چرا نباید عوض می‌شد
5. فقط وقتی مرجع را دوباره ثبت کن (`-Record`) که **مطمئنی** رفتار جدید درست‌تر است،
   و دلیلش را در پیام commit بنویس

---

## تست‌ها چه چیزی را پوشش می‌دهند

| گروه | موضوع | دیتابیس لازم؟ |
|---|---|---|
| ۱ | سریال صورتحساب (Inno) | خیر |
| ۲ | قواعد مشترک MoadianRules | خیر |
| ۲۰ | **قفل رفتار** — تصمیم‌های عمدی که نباید عوض شوند | خیر |
| ۳ تا ۱۲ | ارسال، هویت خریدار، تاریخ، مبالغ، اصلاحی، ابطالی، استعلام | سرور ساختگی |
| ۱۳ تا ۱۵ | نوع صورتحساب، روش تسویه، ماتریس کامل | سرور ساختگی |
| ۱۷ | سازگاری با SDK و قالب JSON روی سیم | سرور ساختگی |
| ۱۶ | ارسال گروهی سنگین، فاکتور بالای یک میلیون | بله |
| ۱۸ | دو محصول: MrCorrect و DenaFaraz | بله |
| ۱۹ | **تثبیت رفتار (Golden)** — payload دقیق نباید عوض شود | بله |
| ۲۱ | مسیر ارسال تکی از سر تا ته | بله |
| ۲۲ | **مقایسه دو مسیر** — تکی و گروهی باید payload یکسان بسازند | بله |
| ۲۳ | ریاضیات فرم صورتحساب اصلاحی | خیر |
| ۲۴ | دکمه ارسال مجدد فاکتور (ردیف TAXDTL و رفتارهای عمدی) | تا ۲۴-۱۲ خیر، بقیه بله |

گروه ۱۹ payload واقعیِ چند فاکتور را با فایل‌های `golden/*.json` مقایسه می‌کند.
اگر یک مبلغ، یک گردکردن یا یک فیلد عوض شود، دقیقاً می‌گوید کدام.

توضیح کامل در `TestTools/LocalMoadian/README.md`.

---

## نکات ساختاری که باید بدانی

**دو محصول، یک کد:**

```
CL_Generaly.MrCorrect = "m"   →  MrCorrect   (پیش‌فرض)
CL_Generaly.MrCorrect = "d"   →  DenaFaraz   (کلید لایسنس تشخیص می‌دهد)
```

شرط `CALLER_NAME == CL_Generaly.MrCorrect` در عمل یعنی «آیا دناافراز نیستیم».

**تگ‌ها:**

```
TAG 2   = حواله انبار فروش   → ردیف‌های کالا اینجاست، همین به مؤدیان می‌رود
TAG 13  = سرگروه فاکتور فروش  → فقط سرصفحه
HEAD_BACK_ANBAR  = ویو روی HEAD_LST با «TAG - 11»
MrCorrect هر دو را دارد، DenaFaraz فقط 2 را.
TAXDTL.TAG همیشه 2 است.
```

**نکته‌ای که ممکن است باگ به نظر برسد ولی نیست:** در `Prg_Grpsend` گرید و
فیلترها با تگ سرگروه کار می‌کنند (`IsDenafaraz ? "2" : "13"`) در حالی که
`TAXDTL.TAG` همیشه ۲ است. یعنی در MrCorrect فیلترِ «قبلاً ارسال شده» عملاً
چیزی پیدا نمی‌کند و همهٔ فاکتورها در لیست می‌مانند.

این آگاهانه نگه داشته شده — روند کاری سال‌هاست روی همین شکل بنا شده و عوض
کردنش ۳٬۴۱۱ فاکتور را از لیست پنهان می‌کند. **دست نزن.** تست ۱۸-۱۰ و ۲۰-۴۳
همین وضعیت را قفل کرده‌اند.

**سه مسیر ارسال مستقل:**

```
گروهی  : Prg_Moadian/Bulk/SendInvoiceBulk.cs        ← بهترین پوشش تست
تکی    : Prg_Moadian/FUNCTIONS/CL_MOADIAN.cs
اصلاحی : Prg_TrackSentInvoice/WIN_MODIFYINVOICE.xaml.cs
```

**ارسال مجدد (`RESEND_BTN` در `Prg_TrackSentInvoice/MainWindow.xaml.cs`):** مسیر
چهارمی است ولی صورتحساب تازه نمی‌سازد — ردیف‌های TAXDTL نسخهٔ پایه را عیناً
دوباره می‌فرستد (همان Taxid، همان Inno، همان تاریخ صدور). چون سامانه گاهی
روزها روی PENDING می‌ماند، یا رد می‌شود چون تکراری است یا پذیرفته می‌شود چون
بار اول نرسیده بود.

نکته‌ای که به آسانی از قلم می‌افتد: `InsertNewTaxDtlRecord` باید **همهٔ** ستون‌های
TAXDTL را ببرد، از جمله `NUMBER` و `TAG`. هر دو nullable هستند، پس اگر جا
بیفتند insert بی‌سروصدا موفق می‌شود و هیچ خطایی دیده نمی‌شود — ولی گرید پیگیری
با `NUMBER` به `HEAD_LST` وصل می‌شود و جستجوی کاربر هم روی `NUMBER` است، پس
ردیف ارسال مجدد بدون شماره فاکتور و بدون نام مشتری ظاهر می‌شود. گروه ۲۴ همین
را قفل کرده و اگر ستونی از دستور درج بیفتد اسمش را می‌گوید.

فرمول‌های مبلغ در این سه جا **تکرار شده‌اند**. اگر یکی را عوض کردی، آن دوتای
دیگر را هم چک کن.

گروه ۲۲ همین را خودکار می‌کند: یک فاکتور را از مسیر تکی و مسیر گروهی می‌فرستد
و payload هر دو را فیلدبه‌فیلد مقایسه می‌کند.

ریاضیات فرم اصلاحی به `Prg_Moadian/FUNCTIONS/CorrectionMath.cs` منتقل شده تا
تست‌پذیر باشد؛ گروه ۲۳ آن را قفل می‌کند و همخوانی‌اش با مسیر گروهی را می‌سنجد.

**یک واگرایی شناخته‌شده و ثبت‌شده:** مسیر تکی و گروهی مالیات را فقط وقتی
مقدار ذخیره‌شده بزرگتر از صفر باشد بازمحاسبه می‌کنند؛ فرم اصلاحی بی‌قید و شرط.
یعنی قلمی که با مالیات صفر روی کالای مالیات‌دار پذیرفته شده، از فرم اصلاحی با
مالیات غیرصفر برمی‌گردد. تست ۲۳-۲۰ این رفتار فعلی را ثبت کرده — اگر خواستید
یکسانش کنید، آن تست به شما می‌گوید چه چیزی عوض می‌شود.

---

## بیلد

```powershell
& "C:\Program Files\Microsoft Visual Studio\2022\Enterprise\MSBuild\Current\Bin\MSBuild.exe" TaxationApp.sln /v:m /nologo
```

`dotnet build` روی پروژه `Prg_Grpsend` شکست می‌خورد (ارجاع COM قدیمی). از MSBuild
ویژوال استودیو استفاده کن.

---

## commit

بدون هماهنگی صریح کاربر **commit یا push نکن**. خواندن و تغییر کد آزاد است.
